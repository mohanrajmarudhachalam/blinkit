import React from "react"
import { StyleSheet, Text, View, Alert, Image, Dimensions, TextInput, TouchableOpacity } from 'react-native';

export function FindAccountPage(): React.ReactElement{


    return(
        <View>
            <Text> Find account page</Text>

        </View>
    )
}